{% extends 'admin/admin_base.html.twig' %}

{% block title %}Edit Skills
{% endblock %}

{% block body %}
	<h1>Edit skill</h1>
	<div>
		{% if skill.media.path is defined %}
			<img src="{{asset('build/' ~ skill.media.path ) }}" alt="test">
		{% else %}
			<img src="{{asset('build/images/icone_default.png') }}" alt="test">
		{% endif %}
		{{include ('admin/skill/_form.html.twig')}}
	</div>

{% endblock %}
