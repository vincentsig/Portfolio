<header>
	<nav class=" customnav navbar navbar-expand-lg navbar-dark">
		<a class="navbar-brand logo" href="{{path('app_home')}}">Vincent Signoret</a>
		<button class="navbar-toggler" type="button" data-toggle="collapse" data-target="#navbarNav" aria-controls="navbarNav" aria-expanded="false" aria-label="Toggle navigation">
			<span class="navbar-toggler-icon"></span>
		</button>
		<div class="collapse navbar-collapse" id="navbarNav">
			<ul class="navbar-nav">
				<li class="nav-item active">
					<a class="nav-link" href="{{path('app_home')}}">Home</a>
				</li>

				{% set home_path = null %}
				{% if app.request.get('_route') != 'app_home' %}
					{% set home_path = path('app_home')	%}
				{% endif %}

				<li class="nav-item">
					<a class="nav-link" href="{{ home_path ~ '#about'}} ">Me</a>
				</li>
				<li class="nav-item">
					<a class="nav-link" href="{{ home_path ~ '#projects'}} ">Projects</a>
				</li>
				<li class="nav-item">
					<a class="nav-link" href="{{ home_path ~ '#contact'}}">Contact</a>
				</li>

			</ul>
		</div>
	</nav>
</header>
